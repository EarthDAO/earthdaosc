// This file serves as the main library file for the project.
// It imports the token contract and exposes its functionality to be used in other parts of the application.

pub mod contracts {
    pub mod token_contract;
}

pub mod types {
    pub mod index;
}